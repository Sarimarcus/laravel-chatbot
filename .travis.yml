language: php

php:
  - 7.0

before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --no-interaction

script:
  - vendor/bin/phpunit

after_success:      # after your build succeeded (tests passed)
  - vendor/bin/dep deploy  # Trigger the deployer